Usage:

./gradlew run (Linux)
gradlew.bat run (Windows, not tested)

Default setup takes ip.txt file provided. To process another file, use

./gradlew run --args "/path/to/file"  (Linux)
./gradlew.bat run --args "C:\\path\\to\\file" (Windows, not tested)
